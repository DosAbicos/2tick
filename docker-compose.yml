services:
  # MongoDB Database
  mongodb:
    image: mongo:7.0
    container_name: 2tick_mongodb
    restart: always
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=signify_db
    networks:
      - 2tick_network

  # FastAPI Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: 2tick_backend
    restart: always
    environment:
      - MONGO_URL=mongodb://mongodb:27017/signify_db
      - DB_NAME=signify_db
      - JWT_SECRET=${JWT_SECRET:-your-super-secret-jwt-key-change-in-production}
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TWILIO_ACCOUNT_SID=${TWILIO_ACCOUNT_SID}
      - TWILIO_AUTH_TOKEN=${TWILIO_AUTH_TOKEN}
      - TWILIO_PHONE_NUMBER=${TWILIO_PHONE_NUMBER}
      - APP_URL=https://2tick.kz
    depends_on:
      - mongodb
    networks:
      - 2tick_network
    volumes:
      - uploads_data:/app/uploads

  # Telegram Bot (separate service)
  telegram_bot:
    build:
      context: ./backend
      dockerfile: Dockerfile.bot
    container_name: 2tick_telegram_bot
    restart: always
    environment:
      - MONGO_URL=mongodb://mongodb:27017/signify_db
      - DB_NAME=signify_db
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
    depends_on:
      - mongodb
    networks:
      - 2tick_network

  # React Frontend
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - REACT_APP_BACKEND_URL=${REACT_APP_BACKEND_URL:-https://2tick.kz}
    container_name: 2tick_frontend
    restart: always
    networks:
      - 2tick_network

  # Nginx Reverse Proxy
  nginx:
    image: nginx:alpine
    container_name: 2tick_nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/ssl:/etc/nginx/ssl:ro
      - certbot_data:/var/www/certbot:ro
      - letsencrypt_data:/etc/letsencrypt:ro
    depends_on:
      - backend
      - frontend
    networks:
      - 2tick_network

  # Certbot for SSL
  certbot:
    image: certbot/certbot
    container_name: 2tick_certbot
    volumes:
      - certbot_data:/var/www/certbot
      - letsencrypt_data:/etc/letsencrypt
    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
    networks:
      - 2tick_network

networks:
  2tick_network:
    driver: bridge

volumes:
  mongodb_data:
  uploads_data:
  certbot_data:
  letsencrypt_data:
