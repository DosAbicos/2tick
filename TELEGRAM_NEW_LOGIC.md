# 🔐 Новая логика Telegram верификации

## Что изменилось

### ❌ Старая логика:
- Один код генерируется при клике кнопки
- Если нажать /start повторно - тот же код
- Если код не работает - нужно заново нажимать кнопку на сайте

### ✅ Новая логика:
- **Приветствие** при первом /start
- **Новый код при каждом /start** (даже 100 раз)
- **Любой непросроченный код работает** для подписания
- Удобно если клиент потерял код или ввел неправильно

---

## Сценарий использования

### 1. Клиент на сайте

```
Страница подписания договора
  ↓
Заполнил данные
  ↓
Загрузил документ
  ↓
Нажал: 💬 Получить код в Telegram
  ↓
Telegram открылся
```

### 2. В Telegram - первый раз

```
Клиент нажимает /start
  ↓
Бот отправляет ДВА сообщения:

[Сообщение 1 - Приветствие]
✅ Добро пожаловать в Signify KZ!

Этот бот отправляет коды подтверждения для подписания договоров.

Сейчас я отправлю вам код...

[пауза 1 секунда]

[Сообщение 2 - Код]
🔐 Код подтверждения:

123456

📋 Нажмите на код чтобы скопировать
🔄 Вернитесь на сайт и вставьте код

⚠️ Код действителен 10 минут

💡 Нужен новый код? Просто нажмите /start снова
```

### 3. Клиент нажимает /start второй раз

**Причины:**
- Потерял первый код
- Ввел неправильно
- Код не скопировался
- Хочет новый код

```
Клиент нажимает /start снова
  ↓
Бот отправляет ОДНО сообщение (только код):

🔐 Новый код:

789012

⚠️ Действителен 10 минут
```

**Важно:** Приветствие НЕ показывается!

### 4. Клиент нажимает /start третий, четвертый... N раз

```
Каждый раз генерируется НОВЫЙ код:
- 123456 (1-й /start)
- 789012 (2-й /start)
- 345678 (3-й /start)
- 901234 (4-й /start)
- и т.д.
```

### 5. Верификация на сайте

**Клиент может использовать ЛЮБОЙ из кодов:**

```
✅ Вводит 123456 → Работает!
✅ Вводит 789012 → Работает!
✅ Вводит 345678 → Работает!
✅ Вводит 901234 → Работает!
```

**Условия:**
- Код не просрочен (10 минут)
- Код еще не использован
- Код для этого контракта

---

## Технические детали

### Backend - генерация кода при /start

**Telegram Bot (`start_telegram_bot.py`):**

```python
async def start(update: Update, context: ContextTypes.DEFAULT_TYPE):
    contract_id = context.args[0]  # Из deep link
    
    # Проверяем - первый раз или нет?
    previous_codes = await db.verifications.count_documents({
        "contract_id": contract_id,
        "method": "telegram"
    })
    
    is_first_time = (previous_codes == 0)
    
    # Приветствие только первый раз
    if is_first_time:
        await update.message.reply_text("🎉 Добро пожаловать...")
        await asyncio.sleep(1)
    
    # Генерируем НОВЫЙ код
    new_code = f"{random.randint(100000, 999999)}"
    
    # Сохраняем в БД
    await db.verifications.insert_one({
        "contract_id": contract_id,
        "otp_code": new_code,
        "method": "telegram",
        "created_at": datetime.now(timezone.utc).isoformat(),
        "expires_at": (datetime.now(timezone.utc) + timedelta(minutes=10)).isoformat(),
        "verified": False
    })
    
    # Отправляем код
    if is_first_time:
        await update.message.reply_text(f"🔐 Ваш код: {new_code}...")
    else:
        await update.message.reply_text(f"🔐 Новый код: {new_code}...")
```

### Backend - верификация любого кода

**API Endpoint (`server.py`):**

```python
@api_router.post("/sign/{contract_id}/verify-telegram-otp")
async def verify_telegram_otp(contract_id: str, data: dict):
    entered_code = data.get('code')
    
    # Ищем ЛЮБОЙ код (не только последний)
    verification = await db.verifications.find_one({
        "contract_id": contract_id,
        "method": "telegram",
        "otp_code": entered_code,  # Точное совпадение
        "verified": False           # Еще не использован
    })
    
    if not verification:
        # Проверяем - может уже использован?
        used = await db.verifications.find_one({
            "contract_id": contract_id,
            "method": "telegram",
            "otp_code": entered_code,
            "verified": True
        })
        
        if used:
            raise HTTPException(400, "Этот код уже использован")
        else:
            raise HTTPException(400, "Неверный код")
    
    # Проверяем срок действия
    expires_at = datetime.fromisoformat(verification['expires_at'])
    if datetime.now(timezone.utc) > expires_at:
        raise HTTPException(400, "Код истек")
    
    # Код валидный - помечаем как использованный
    await db.verifications.update_one(
        {"_id": verification['_id']},
        {"$set": {"verified": True}}
    )
    
    # Подписываем договор
    # ...
    
    return {"verified": True, "signature_hash": "..."}
```

### База данных - множественные коды

**MongoDB - коллекция `verifications`:**

```javascript
// Пример для одного контракта после 3-х /start

[
  {
    "_id": ObjectId("..."),
    "contract_id": "abc-123",
    "otp_code": "123456",
    "method": "telegram",
    "created_at": "2025-10-21T19:00:00Z",
    "expires_at": "2025-10-21T19:10:00Z",
    "verified": false  // Еще можно использовать
  },
  {
    "_id": ObjectId("..."),
    "contract_id": "abc-123",
    "otp_code": "789012",
    "method": "telegram",
    "created_at": "2025-10-21T19:01:00Z",
    "expires_at": "2025-10-21T19:11:00Z",
    "verified": false  // Еще можно использовать
  },
  {
    "_id": ObjectId("..."),
    "contract_id": "abc-123",
    "otp_code": "345678",
    "method": "telegram",
    "created_at": "2025-10-21T19:02:00Z",
    "expires_at": "2025-10-21T19:12:00Z",
    "verified": false  // Еще можно использовать
  }
]
```

**После использования кода 789012:**

```javascript
[
  {
    "otp_code": "123456",
    "verified": false  // ✅ Все еще можно использовать
  },
  {
    "otp_code": "789012",
    "verified": true   // ❌ Уже использован
  },
  {
    "otp_code": "345678",
    "verified": false  // ✅ Все еще можно использовать
  }
]
```

---

## Примеры сообщений об ошибках

### 1. Неверный код
```
❌ Неверный код. Проверьте код в Telegram или запросите новый.
```

### 2. Код уже использован
```
❌ Этот код уже использован. Запросите новый код.
```
*Решение:* Нажать /start в боте для нового кода

### 3. Код истек
```
❌ Код истек. Нажмите /start в боте для нового кода.
```
*Код действителен 10 минут*

---

## Преимущества новой логики

### ✅ Для пользователя:

1. **Гибкость**: Можно запросить код сколько угодно раз
2. **Удобство**: Потерял код? Просто нажми /start снова
3. **Надежность**: Любой непросроченный код работает
4. **Простота**: Не нужно возвращаться на сайт за новым кодом

### ✅ Для системы:

1. **Меньше нагрузки**: Не нужно заново генерировать deep link
2. **Лучший UX**: Все в одном месте (Telegram)
3. **Безопасность**: Коды истекают через 10 минут
4. **Аудит**: Видно сколько раз пользователь запросил код

---

## Тестирование

### Создать тестовый контракт:

```bash
bash /tmp/test_new_logic.sh
```

### Или вручную:

1. Откройте тестовую ссылку
2. Нажмите кнопку "Получить код в Telegram"
3. В Telegram нажмите /start
4. Скопируйте код
5. **НЕ ВСТАВЛЯЙТЕ ЕЩЕ**
6. Нажмите /start снова
7. Получите другой код
8. Нажмите /start третий раз
9. Получите третий код
10. **Теперь попробуйте вставить ЛЮБОЙ из 3-х кодов**
11. ✅ Все работают!

### Проверка логов бота:

```bash
tail -f /tmp/telegram_bot.log
```

Должно быть:
```
✅ Generated and sent NEW OTP 123456 to user for contract abc-123 (Attempt #1)
✅ Generated and sent NEW OTP 789012 to user for contract abc-123 (Attempt #2)
✅ Generated and sent NEW OTP 345678 to user for contract abc-123 (Attempt #3)
```

---

## Итог

**Старая логика:**
- 1 код → 1 попытка
- Не работает? Начинай сначала с сайта

**Новая логика:**
- Неограниченно кодов в Telegram
- Любой код работает (пока не истек)
- Удобно и надежно!

🎉 **Готово к использованию!**
